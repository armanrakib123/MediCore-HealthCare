services:
  - type: web
    name: MediCore-api
    runtime: docker
    rootDir: MediCore.API
    plan: free
    autoDeploy: true
    healthCheckPath: /health
    envVars:
      - key: ASPNETCORE_ENVIRONMENT
        value: Production
      - key: MongoDbSettings__ConnectionString
        sync: false
      - key: MongoDbSettings__DatabaseName
        value: MediCore
      - key: Jwt__Key
        generateValue: true
      - key: Jwt__Issuer
        value: MediCore
